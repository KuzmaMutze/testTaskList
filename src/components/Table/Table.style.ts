import makeStyles from '@mui/styles/makeStyles/makeStyles';

export const useStyles = makeStyles({
    container: {
        height: '100%',
    },
    table: {
        width: '100%',
        height: 650,
        overflow: 'auto',
    },
});
